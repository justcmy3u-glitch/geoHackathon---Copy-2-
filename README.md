# KMG GeoEvidence Copilot

Evidence-grounded document AI for geological reports.

## Setup

1. Copy `.env.example` to `.env` and fill in `COLAB_TUNNEL_URL` after running the Colab notebook.
2. Run `docker compose up --build -d`.
3. Open `http://localhost:8501`.
